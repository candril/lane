# Dependencies
node_modules/

# Build output
dist/

# TypeScript
tsconfig.tsbuildinfo

# Environment
.env
.env.local

# OS
.DS_Store

# Local editor / agent state
.vim/
.claude/settings.local.json

# Local-only just recipes
local.just

# Nix: the flake is deliberately unlocked (see flake.nix)
flake.lock
